# @kjots/test-dependency

## Test dependency

This package is the test dependency for the project.  It contains a peer dependency on `angucomplete-alt@3.0.0` which
itself contains a cross-site scripting issue.
